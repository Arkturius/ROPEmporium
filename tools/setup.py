#!/usr/bin/python3

from setuptools import setup, find_packages

setup(
    name="ropchain",
    version="0.1",
    packages=find_packages(),
    description="ROP utility functions",
    author="rgramati",
)
